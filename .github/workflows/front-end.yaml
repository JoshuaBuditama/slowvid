name: front-end
on:
  push:
jobs:
  front-end:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm install
        working-directory: front-end
      - name: Build
        run: npm run build
        working-directory: front-end
